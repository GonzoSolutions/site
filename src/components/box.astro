---
const { color, padding, radius, _case } = Astro.props;
---
<box style={`background: ${color}`} class="padded rounded aligned cased" padding={padding} radius={radius} alignment="center" case={_case}>
    <slot />
</box>

<style>

box {
    display: block;
    overflow: hidden;
}

</style>