---
const { color, padding, radius } = Astro.props;
---
<box style={`background: ${color}`} class="padded rounded" padding={padding} radius={radius}>
    <slot />
</box>

<style>

box {
    display: block;
    overflow: hidden;
}

</style>